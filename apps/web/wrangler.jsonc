{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "crazy-web",
  "main": "@tanstack/react-start/server-entry",
  "compatibility_date": "2026-09-01",
  "compatibility_flags": ["nodejs_compat"],
  "observability": { "enabled": true },
  // The per-user Coordinator lives in the core Worker. Every write, and every
  // WebSocket upgrade, is handed to it through this binding.
  "durable_objects": {
    "bindings": [
      { "name": "COORDINATOR", "class_name": "Coordinator", "script_name": "crazy-core" },
    ],
  },
  // Attachments. This Worker is the one that moves the bytes, because it is the
  // one that authenticates the request and has the body in its hands; the
  // Coordinator decides and records the metadata and never touches a bucket
  // (ADR 0002). The bucket is private: no public URL, no r2.dev domain, and
  // every read goes through `GET /attachments/:id` behind the user's own row.
  "r2_buckets": [{ "binding": "ATTACHMENTS", "bucket_name": "crazy-attachments" }],
  // Read only. The web app never writes to D1 (ADR 0002); migrations are
  // listed here so `pnpm db:migrate` lands in the state directory `vp dev` uses.
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "crazy",
      // The `crazy` database on Cloudflare. Must match apps/core.
      "database_id": "c8e2a6ab-356a-4f60-a195-8eb1c8265c9a",
      "migrations_dir": "../../packages/db/migrations",
    },
  ],
}
