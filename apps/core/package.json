{
  "name": "@crazy/core",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "db:migrate:remote": "wrangler d1 migrations apply crazy --remote",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@crazy/db": "workspace:*",
    "@crazy/shared": "workspace:*",
    "agents": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "wrangler": "catalog:"
  }
}
